name: REALMat — verificar Basic Analysis I

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  build:
    uses: projetorealmat/.github/.github/workflows/book-ci.yml@v2
    with:
      config: .realmat/book.json
      commit: ${{ github.event.pull_request.head.sha || github.sha }}
